# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
.terraform.lock.hcl.bak
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Bootstrap keeps LOCAL state -- never commit it, it is not encrypted.
bootstrap/terraform.tfstate
bootstrap/terraform.tfstate.backup

# Sensitive: variable files may contain account IDs / ARNs
*.auto.tfvars
secrets.tfvars

# AWS / credentials -- never commit
.aws/
credentials
*.pem
*.key

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Spec + secrets for the web app -- never commit
Election monitor web app prompt.docx
admin user and pwd.txt
safe harbour statement.docx
prompt-v2.docx
Design Diagram.drawio
~$*.xlsx
~$*.docx
osun-polling-units.xlsx
osun-polling-units.csv
presidential-polling-units.xlsx
presidential-polling-units.csv
# The raw INEC dataset the national list is built from (35 MB). Kept locally so
# tools/build_national_pu.py can be re-run without re-downloading; the source
# URL is in the README if it is ever lost.
inec-source-data.json
.admin-ssm.json

# Lambda build artefact
infra/.build/

# Generated sample photos
tools/seed/

# Textract fixture is a captured API response, kept for the parser test
!tools/fixtures/
